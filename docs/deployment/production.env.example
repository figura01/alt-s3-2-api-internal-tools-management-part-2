# Copy to .env.production at the repository root; never commit secrets.
# Actual existing network used by Caddy (inspect on the VPS).
CADDY_WEB_NETWORK=
# Generate two independent values: openssl rand -hex 32
POSTGRES_PASSWORD=
JWT_SECRET=
