# Vérification du staging — 23 septembre 2026

## État observé

- URL : https://staging.techcorp.laurent-vuillaume.ovh
- Le staging était déjà déployé à l'arrivée ; aucun redéploiement effectué.
- Dépôt VPS `/srv/apps/techcorp` propre, au commit `cef5911`, identique au HEAD local.
- Frontend et API démarrés ; PostgreSQL déclaré sain par Docker.
- Les 11 migrations Prisma sont appliquées, y compris les notifications et événements.
- Caddy utilise le réseau `vps-infra_web` et dispose du bloc staging.
- Page `/login` accessible en HTTPS (200), formulaire confirmé dans le navigateur.
- `/api/departments` répond 200 ; sans session, `/api/tools`, `/api/analytics`, `/api/notifications` et `/api/auth/me` répondent 401.
- Le portfolio et test-projet-1 répondent chacun 200 en HTTPS.
- Base staging vide : aucun utilisateur, département, catégorie ou outil.

## Vérifications locales

- Backend : 22 suites, 172 tests réussis ; compilation réussie.
- Frontend : 11 tests réussis ; lint sans erreur (72 avertissements) ; compilation et vérification des types réussies.
- Les tests HTTP backend ont nécessité l'autorisation d'ouvrir des ports locaux. La compilation frontend a nécessité l'accès réseau à Google Fonts.
- La CI distante n'a pas été vérifiée. Les parcours E2E complets n'ont pas été exécutés dans cette passe ; Docker local n'était pas démarré.

## Reste à valider

- Premier administrateur créé le 24 septembre (voir ci-dessous).
- Vérifier les parcours authentifiés en HTTPS : connexion, renouvellement, déconnexion, permissions, mutations d'outils, CSV et notifications.
- Préparer et tester sauvegarde/restauration avant la production.
- Le VPS dispose de 2 Go de disque libre (91 % utilisé), de 1,9 Gio de RAM et d'aucun swap : examiner la capacité avant une nouvelle compilation sur place. Aucune image, aucun cache et aucun volume n'ont été supprimés.

Ces contrôles ne constituent pas une validation de mise en production.

## Compte administrateur — 24 septembre 2026

- Compte `laurent@test.com` créé avec rôle ADMIN, statut ACTIVE et département Administration.
- Mot de passe aléatoire conservé localement dans `.tmp/staging-admin.json`, ignoré par Git, avec permissions 600 ; répertoire en 700. Aucun secret n'est inclus dans ce rapport.
- Vérifications réelles de l'API publique en HTTPS : connexion 201, profil ADMIN 200, catalogue/Analytics/utilisateurs/notifications 200, renouvellement 201, déconnexion 201, puis profil refusé 401.
- Les deux cookies de session sont Secure et HttpOnly. Validation TLS maintenue avec les certificats système macOS.
- Aucun seed de démonstration exécuté. Les vérifications des mutations d'outils, des rôles MANAGER/EMPLOYEE et de l'export CSV dans le navigateur restent à effectuer.
